{
  "name": "vite-ssr-hydrojs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server",
    "build": "npm run build:client && npm run build:server",
    "build:client": "vite build --outDir dist/client",
    "build:server": "vite build --ssr src/entry-server.tsx --outDir dist/server",
    "preview": "cross-env NODE_ENV=production node server"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "hydro-js": "^1.8.14",
    "sirv": "^3.0.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.0",
    "cross-env": "^10.1.0",
    "hydro-js-integrations": "^0.1.5",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  }
}
